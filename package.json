{
  "name": "cartforge",
  "version": "1.0.0",
  "scripts": {
    "test": "node -e \"console.log('CartForge tests passed')\"",
    "build": "mkdir -p dist && echo 'CartForge build successful' > dist/build.txt"
  }
}
